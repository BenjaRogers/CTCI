#include <iostream>
// rotate matrix by 90 degrees
int* rotateMatrix(int matrix[]) {

}

int main() {
    // Generate 10x10 byte array of ints (4bytes)
    int byteArray[10][10];
    for (int i=0; i<10; i++) {
        for (int j=0; j<10; j++) {
            byteArray[i][j] = i * 10 + j;
        }
    }
    // for (int i=0; i<10; i++) {
    //     for (int j=0; j<10; j++) {
    //         std::cout << byteArray[i][j];
    //     }
    // }
}